
CREATE DATABASE usersinfo;
USE usersinfo;
CREATE DATABASE xpozed_db
USE xpozed_db;

-- CREATE TABLE `users` (
--   `id` Int( 11 ) AUTO_INCREMENT NOT NULL,
--   `author` VARCHAR( 255) NOT NULL,
--   `email` VARCHAR( 255 ) NOT NULL,
--   `created_at` DATETIME NOT NULL,
--   `password` VARCHAR( 255 ) NOT NULL,
--   'jobTitle' VARCHAR( 55 ) NOT NULL,
--   `credibility` VARCHAR( 55 ) NOT NULL,

--   PRIMARY KEY ( `id` ) 
-- );

-- CREATE TABLE `posts` (
--   `id` Int( 11 ) AUTO_INCREMENT NOT NULL,
--   `author` VARCHAR( 255) NOT NULL,
--   `created_at` DATETIME NOT NULL,
--   `topic` VARCHAR( 55) NOT NULL,
--   `vote_rank` Int,
 
--   PRIMARY KEY ( `id` ) 
-- );